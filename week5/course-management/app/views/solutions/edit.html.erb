<h2>Edit solution for task: <%= @task.name %></h2>
<%= form_for [@lecture, @task, @solution] do |f|%>
  <%= f.label :body %>
  <%= f.text_field :body %><br>
  <%= f.submit 'Update Solution'%>
<% end %>
